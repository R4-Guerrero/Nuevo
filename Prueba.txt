Archivo de pruebas.
 El contenido del archivo sufrirá algunos cambios.
 Nota que hay un error en la extensión del archivo en la línea 1.
 Más adelante vamos a resolver ese error. 
. En la rama “ramaNueva” edita el archivo “Prueba.txt”, añade al final una nueva línea. Guarda el archivo, registra los cambios con “git add”, realiza el
 commit y al finalizar cambia a la rama “master” (notarás que los cambios desaparecen), si regresas a la rama “ramaNueva” el cambió aparecerá. 